package zoot.arbre.expressions.Binaire;

import zoot.arbre.expressions.Expression;

public abstract class Binaire extends Expression {
    /**
     * Constructeur
     *
     * @param n
     * @param num
     */
    protected Binaire(int n, int num) {
        super(n, num);
    }
}
